PORT=4000

# PostgreSQL connection string
DATABASE_URL=postgresql://username:password@localhost:5432/crypto_exchange_sim

# Redis connection string
REDIS_URL=redis://default:********@handy-mastodon-104903.upstash.io:6379

# JWT secrets — use long random strings in production (e.g. `openssl rand -hex 64`)
JWT_ACCESS_SECRET=replace_with_a_long_random_secret
JWT_REFRESH_SECRET=replace_with_a_different_long_random_secret
